name: CI — Build & Push

on:
  push:
    branches: [main]
    paths:
      - "app/**"
      - "Dockerfile"

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_PAT }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
        # QEMU emulates ARM on the GitHub Actions runner (which is AMD64)
        # This is what allows building for multiple architectures

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        # Buildx is the Docker plugin that enables multi-platform builds

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push (amd64 + arm64)
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          # Builds for both Intel/AMD machines and ARM devices (Pi, Apple Silicon, etc.)
          platforms: linux/amd64,linux/arm64
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest

      - name: Update image tag in dev/deployment.yaml
        run: |
          sed -i "s|${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:.*|${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}|g" dev/deployment.yaml

      - name: Commit new tag
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add dev/deployment.yaml
          git commit -m "ci: update image tag to ${{ github.sha }} [skip ci]"
          git push