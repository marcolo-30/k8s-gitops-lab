apiVersion: v1
kind: Service
metadata:
  name: image-processor-svc # Renamed from main-app-service
  namespace: myapp
spec:
  type: NodePort
  selector:
    app: image-processor # Must match the new labels
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30007 # Using the original NodePort
