apiVersion: v1
kind: Service
metadata:
  name: main-app-service
  namespace: myapp
spec:
  type: NodePort
  selector:
    app: main-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30007
